const page = ({params}: {params: {post: string}}) => {
console.log("🚀 ~ file: page.tsx:2 ~ page ~ searchParams:", params.post)

  return (
    <div>
      Enter
    </div>
  );
}

export default page;